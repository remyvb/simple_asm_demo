---
version: 5

defaults:
  datadir: /etc/puppetlabs/code/environments/production/hieradata
  data_hash: yaml_data

hierarchy:
  - name: "Per node data"
    path: "nodes/%{fqdn}.yaml"
  - name: "Per deployment zone"
    path: "%{deployment_zone}.yaml"
  - name: "The defaults"
    path: "defaults.yaml"
